---
layout: layouts/base.njk
permalink: /
---

<section class="hero">
  <div class="hero-badge">{{ homepage.hero.badge }}</div>
  <div class="hero-title">{{ homepage.hero.headlineBefore }}<br><em><span class="highlight">{{ homepage.hero.headlineHighlight }}</span></em></div>
  <p>{{ homepage.hero.description }}</p>
  <a href="{{ site.chromeStoreUrl }}" class="hero-cta">
    <span class="hero-cta-icon" aria-hidden="true">{{ "hamsa-white.svg" | inlineSvg | safe }}</span>
    {{ homepage.hero.cta }}
  </a>
</section>

<section class="problem">
  <div class="problem-inner">
    {% for paragraph in homepage.problem.paragraphs %}
    <p>{{ paragraph | safe }}</p>
    {% endfor %}
  </div>
</section>

<section class="section">
  <div class="section-title">{{ homepage.steps.title }}</div>
  <div class="steps">
    {% for step in homepage.steps.items %}
    <div class="step">
      <div class="step-num">{{ step.number }}</div>
      <div class="step-title">{{ step.title }}</div>
      <p>{{ step.description }}</p>
    </div>
    {% endfor %}
  </div>
  <p class="steps-footnote">{{ homepage.steps.footnote }}</p>
</section>

<section class="features-bg">
  <div class="features-inner">
    <div class="features-title">{{ homepage.features.title }}</div>
    <div class="features-intro">
      <p>{{ homepage.features.intro }}</p>
    </div>
    {% include "partials/feature-grid.njk" %}
  </div>
</section>
